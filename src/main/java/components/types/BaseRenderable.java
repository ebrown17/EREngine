package components.types;

import maps.TileType;
import util.RenderPriority;

public class BaseRenderable extends Renderable{

	public BaseRenderable(Position position, TileType tile, RenderPriority priority) {
		super(position, tile, priority);
		// TODO Auto-generated constructor stub
	}

}

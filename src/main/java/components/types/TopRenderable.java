package components.types;

import maps.TileType;
import util.RenderPriority;

public class TopRenderable extends Renderable{

	public TopRenderable(Position position, TileType tile, RenderPriority priority) {
		super(position, tile, priority);
		// TODO Auto-generated constructor stub
	}

}

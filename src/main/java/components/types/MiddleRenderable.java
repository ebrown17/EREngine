package components.types;

import maps.TileType;
import util.RenderPriority;

public class MiddleRenderable extends Renderable{

	public MiddleRenderable(Position position, TileType tile, RenderPriority priority) {
		super(position, tile, priority);
		// TODO Auto-generated constructor stub
	}

}
